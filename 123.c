#include<stdio.h>

int main (void)
{
    int length ,width , height, volume;
    length = 10;
    width  =5;
    height =10;
    volume =length * width*height;
    printf("Aera is %d!\n", volume);
    return 0;
}